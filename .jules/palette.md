## 2024-05-18 - Missing ARIA Labels on Close Buttons
**Learning:** Icon-only close buttons (like "✕") across various modal and notification components in this project frequently lack `aria-label` attributes, making them inaccessible to screen readers.
**Action:** Always verify that newly added or existing icon-only buttons include descriptive `aria-label`s.
